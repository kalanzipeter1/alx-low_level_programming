#include <stdio.h>
/**
 *main- main entry point,returns 0
 *printf- prints with proper grammar, but the outcome is a piece of art,
 *Return: always zero (success)
 *
 */
int main(void)
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
